feat: enrich ride builder and sync contact form options

🇬🇧 EN:
- Enriched the Ride Builder in `tours.html` by adding descriptive subtitles to all filter buttons (Level, Group, Duration, Ride type) to explain their meaning clearly.
- Added a new 'Extended (6-12 hours)' duration option to the Ride Builder.
- Improved the layout of the Ride Builder on large screens by shifting from a 50/50 layout to a wider left column (7/12) and using CSS Grid for the button containers. This ensures all buttons fit on a single row without wrapping.
- Synchronized the 'Group Size' options in `contact.html` with the updated Ride Builder options.
- Updated the Javascript mapping logic in the contact form so that selections made in the Ride Builder properly pre-fill the form upon redirection.

🇺🇦 UA:
- Покращено блок конструктора кастомних турів (Ride Builder) у `tours.html`: до всіх кнопок фільтрів додано короткі пояснювальні підзаголовки (наприклад, рівень складності, кількість людей у групі, тривалість у годинах).
- Додано нову опцію тривалості туру 'Extended (6-12 hours)'.
- Оптимізовано верстку конструктора для широких екранів: лівій колонці виділено більше місця, а контейнери кнопок переведено на CSS Grid. Завдяки цьому кнопки завжди вишиковуються в один ряд і не переносяться на новий рядок.
- Синхронізовано значення поля 'Group Size' у формі контактів (`contact.html`) з новими значеннями з конструктора.
- Оновлено JavaScript-мапінг у `contact.html`, щоб вибір користувача в конструкторі автоматично та коректно переносився до контактної форми при переході на неї.
