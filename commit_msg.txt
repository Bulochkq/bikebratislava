feat: add readme, htaccess, favicons and normalize picture names

🇬🇧 EN:
- Renamed all image files in the `pictures` directory to use consistent kebab-case format.
- Updated all HTML files, `app.js`, and `sitemap.xml` to point to the new image filenames.
- Added `.htaccess` for server configuration (HTTPS, caching, gzip).
- Added `README.md` with detailed project structure and setup instructions.
- Added `privacy.html` and necessary favicons.
- Removed obsolete files like the blueprint and intro.mp4.

🇺🇦 UA:
- Перейменовано всі файли зображень у каталозі `pictures` на єдиний формат kebab-case.
- Оновлено всі HTML-файли, `app.js` та `sitemap.xml`, щоб вони вказували на нові імена зображень.
- Додано `.htaccess` для налаштування сервера (HTTPS, кешування, gzip).
- Додано `README.md` із детальним описом структури проєкту та інструкціями.
- Додано `privacy.html` та необхідні фавіконки.
- Видалено застарілі файли (креслення сайту та intro.mp4).
