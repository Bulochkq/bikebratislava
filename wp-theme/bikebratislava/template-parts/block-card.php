<?php
/**
 * Картка сама по собі нічого не виводить: вона показується всередині
 * свого надрядного блоку (block-cards.php).
 *
 * Файл потрібен, щоб get_template_part() не шукав його марно, і щоб
 * картка, яку помилково залишили без надрядного блоку, просто не
 * з'являлась на сторінці замість того, щоб ламати верстку.
 */

if (!defined('ABSPATH')) {
    exit;
}
